[project]
name = "worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "rpyc>=6.0.0",
    "redis>=5.0.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "fakeredis>=2.23.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
